#![allow(missing_docs, clippy::unwrap_used, clippy::expect_used, clippy::indexing_slicing)]

mod fuzz;
mod hardfork_config;
mod integration;
mod p2p;
mod rpc_compat;
mod testsuite;

const fn main() {}
